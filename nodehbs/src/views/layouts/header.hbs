<header>
  <nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/"><img src="assets/img/logo2.png" width="90px" height="60px"/></a>
      
      <button
        class="navbar-toggler"
        type="button"
        data-bs-toggle="collapse"
        data-bs-target="#navbarCollapse"
        aria-controls="navbarCollapse"
        aria-expanded="false"
        aria-label="Toggle navigation"
      >
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarCollapse">
        <ul class="navbar-nav me-auto mb-2 mb-md-0">
          <li class="nav-item">
            <a class="nav-link {{#if active.Home}}active{{/if}}" aria-current="page" href="/">Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link {{#if active.Compra}}active{{/if}}" aria-current="page" href="/compra">Compra un cotxe</a>
          </li>
          <li class="nav-item">
            <a class="nav-link {{#if active.Venta}}active{{/if}}" aria-current="page" href="/venta">Ven el teu cotxe</a>
          </li>
          <li class="nav-item">
            <a class="nav-link {{#if active.Financiar}}active{{/if}}" aria-current="page" href="/financiar">Financia amb nosaltres !</a>
          </li>
                    <li class="nav-item">
            <a class="nav-link {{#if active.Contacte}}active{{/if}}" aria-current="page" href="/contacte">Contacte</a>
          </li>

        </ul>
          {{#if user}}
          <p style="color: rgba(255,255,255,.55); margin-right: 10px" class="mb-0">Benvolgut/da, <p class="mb-0" style="color: azure; margin-right: 10px">{{user}}</p></p>
          <button class="btn btn-outline-secondary" aria-current="page"><a style="color: rgba(255,255,255,.55);" class="nav-link {{#if active.Login}}active{{/if}}" aria-current="page" href="/logout">Logout</a></button>
           {{else}}
           <button class="btn btn-outline-secondary" aria-current="page"><a style="color: rgba(255,255,255,.55);" class="nav-link {{#if active.Login}}active{{/if}}" aria-current="page" href="/login">Login</a></button>
           {{/if}}
           {{#if esAdmin}}
          <button class="btn btn-outline-secondary" aria-current="page"><a style="color: rgba(255,255,255,.55);" class="nav-link {{#if active.Admin_dashboard}}active{{/if}}" aria-current="page" href="/admin_dashboard">Administrar</a></button>
           {{/if}}
      </div>
    </div>
  </nav>
</header>